# Leave empty in dev — Vite proxy forwards /api to the backend.
# Local dev: http://127.0.0.1:8000
# Docker:    http://host.docker.internal:8000 (set in compose.yaml)
VITE_API_URL=

# Override Vite proxy target (optional)
# VITE_PROXY_TARGET=http://127.0.0.1:8000
